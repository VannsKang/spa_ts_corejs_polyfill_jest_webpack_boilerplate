import User from '../components/User';
import 'whatwg-fetch';

// SECTION REST CR API Check
describe('Checking Components', () => {
  test('User Component exists', () => {
    expect(User).toBeDefined();
  });
});
