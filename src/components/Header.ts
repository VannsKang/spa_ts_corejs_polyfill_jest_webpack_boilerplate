const Header = () => {
  const template = `
    <header>
      <h1>My SPA App</h1>
      <p>This is a boilerplate for a simple TS workflow using webpack</p>
    </header>
  `;

  return template;
};

export default Header;
